services:
  tests:
    image: buildkite/plugin-tester:v4.3.0
    volumes:
      - ".:/plugin"

  lint:
    image: buildkite/plugin-linter:v2.1.0
    command: ['--id', 'custom-checkout']
    volumes:
      - ".:/plugin:ro"